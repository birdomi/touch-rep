# @package _global_

# W=5 temporal encoder on 15-frame windows, CLS readout: the former emits one
# DINO-trained CLS per 5-frame encoder window (3 per sample), MeanPoolProbe
# averages the three and classifies.

defaults:
  - /experiment/brainco/ours_3d/task/slip_detection/temporal_w15_seq5
  - _self_

experiment_name: brainco_xyz_slip_detection_temporal_w15_seq5_cls

wandb:
  group: temporal_w15_seq5_cls_slip
  tags: ["brainco", "xyz", "slip_detection", "temporal_former", "w15", "seq5", "cls"]

task:
  pooling: cls
