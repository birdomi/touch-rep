# @package _global_

defaults:
  - /experiment/brainco/ours_3d/dinov2_prediction
  - _self_

model_size: small
experiment_name: dinov2_prediction_global1_all_pseudo_force_${model_size}_rope_temporal3

wandb:
  group: dino_prediction_global1_all_pseudo_force_${model_size}_rope_temporal3
  tags: ["all", "pseudo_force", "4d", "${model_size}", "rope", "temporal3", "prediction", "global_1view"]

algorithm:
  prediction_context: global

  encoder:
    _target_: tactile_ssl.model.angle_small

  predictor:
    predictor_dim: 384
    num_heads: 6
