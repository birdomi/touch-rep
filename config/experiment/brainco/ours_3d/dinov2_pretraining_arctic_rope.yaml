# @package _global_

defaults:
  - /experiment/brainco/ours_3d/dinov2_pretraining_all_rope
  - _self_

experiment_name: dinov2_arctic_pseudo_force_${model_size}_rope

wandb:
  group: dino_arctic_pseudo_force_${model_size}_rope
  tags: ["arctic", "pseudo_force", "4d", "xyz", "${model_size}", "rope"]

data:
  sensor: compact_pseudo_force
  data_root: ./pretraining_dataset/pseudo_force_compact/arctic

algorithm:
  sensor_input_key: joint_contact
