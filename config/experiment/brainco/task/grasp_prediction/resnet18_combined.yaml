# @package _global_
defaults:
  - override /data: brainco_grasp_prediction_combined_vision
  - override /task: brainco_grasp_prediction
  - _self_

sensor: brainco_vision
experiment_name: combined_grasp_prediction_resnet18
ckpt_path: ./
resume_id: ~

ssl_name: resnet18
checkpoint_encoder: ~

fold: 0
num_folds: 4

pretrained: true
freeze_backbone: false
use_scheduler: true

train_data_budget: 1.0
val_data_budget: 1.0

wandb:
  project: brainco_grasp_prediction
  group: combined_grasp_prediction_resnet18
  tags: ["brainco", "vision", "resnet18", "combined", "grasp_prediction"]
