# @package _global_
defaults:
  - /paths: ~
  - /wandb: ~
  - /data: xela_relative_pose
  - /task: xela_relative_pose
  - _self_

ssl_name: dinov2_finetune
sensor: xela
ckpt_path: ~
resume_id: ~
train_data_budget: 1.0
val_data_budget: 1.0

trainer:
  save_probe_weights_only: false

task:
  checkpoint_encoder: ${paths.encoder_checkpoint_root}/dinov2_nopatch_sensorpose_500epochs.ckpt
  train_encoder: true
