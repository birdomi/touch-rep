# BrainCo downstream tasks

This package now keeps BrainCo downstream modules only.

Useful entrypoints:

```bash
python train_task_brainco.py +experiment=brainco/task/grasp_prediction/dinov2.yaml
python train_task_brainco_angle.py +experiment=brainco/ours_vectors/task/grasp_prediction/dinov2_multi_combined.yaml
```
